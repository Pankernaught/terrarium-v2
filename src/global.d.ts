// Allow side-effect CSS imports (the Expo template imports `@/global.css` for
// react-native-web styling). Native platforms ignore these at runtime; this just
// satisfies the TypeScript checker.
declare module '*.css';
